<div class="w-full h-full bg-zinc-900 inset-0">
    {{-- @for ($search as $data)
        <x-card
        :img="$data->img"
        :title="$data->title"
        :excerpt="$data->excerpt"
        :time="$data->time"
        :unread="$data->unread"
        />
    @endfor --}}
</div>